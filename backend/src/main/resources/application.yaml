spring:
  application:
    name: univer-demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${env.mysql.host}:${env.mysql.port}/univer_db
    username: univer_db_user
    password: pass
  grpc:
    server.port: ${env.grpc.port}